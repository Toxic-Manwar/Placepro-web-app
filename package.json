{
  "name": "placepro-root",
  "private": true,
  "workspaces": [
    "placepro"
  ],
  "scripts": {
    "dev": "npm run dev --prefix placepro",
    "build": "cd placepro && npm install && npm run build",
    "lint": "npm run lint --prefix placepro",
    "preview": "npm run preview --prefix placepro"
  }
}
